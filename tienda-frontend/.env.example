# Desarrollo local: Vite redirige /api hacia Laravel.
# En Railway configurar VITE_API_URL=https://api.minizon.store/api.
# Las variables VITE_* quedan incorporadas durante npm run build.
# No colocar aqui contrasenas de base de datos ni claves de IA.
VITE_API_URL=/api
